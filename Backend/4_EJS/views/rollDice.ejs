<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
<h1>Dice gave value:
    <%= diceVal %>
</h1>
<h1>
<%# we refer ejs in "ejs.co" %>
<%# This is a ejs comment, adding a loop statement in ejs to roll dice again if it is equal to 6 %>
    <% if (diceVal === 6) { %>
        Rolling again...
        <%- include('rollDice', { diceVal: Math.floor(Math.random() * 6) + 1 }) %>
    <% } else { %>
        No more rolls.
    <% } %>

</h1>
</body>
</html>